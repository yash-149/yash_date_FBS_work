// find the length of the string using built in function..

#include<stdio.h>
#include<string.h>

void main()
{
	char str[10];
	printf("enter a string:");
	scanf("%s",str);
	printf("%s",str);
	printf("\nlength of the string is:%d",strlen(str));
	
}
